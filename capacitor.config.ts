import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vibrantwolfglow',
  appName: 'vibrant-wolf-glow',
  webDir: 'dist'
};

export default config;
